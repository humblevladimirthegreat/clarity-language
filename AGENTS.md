# Clarity Language

Constructed language and tooling that encode psychologically useful distinctions into vocabulary and grammar (compassion / rationality / empowerment themes: gratitude, bias awareness, authentic choice). Unambiguous syntax supports computational tools; phonology aims to be easy to sing.

Always call the language **Clarity** (never Geran or other legacy names).

This repo is TypeScript tooling around the language docs and lexicon. A web UI may come later.

## Sources of truth

| Source | Role |
|--------|------|
| `docs/introduction.md` | Purpose (compassion / rationality / empowerment), grammar design goals, learning path, feature criteria. |
| `docs/language-reference.md` | **Source of truth** for core clause grammar (PoS, orthography, utterance framing, …). Revise it as design decisions land. Linked grammar docs own their subsystems. |
| `docs/meta/learning-levels.md` | Rubric for tagging grammar docs **beginner** / **intermediate** / **advanced** (cross-doc learning path — pedagogy, not design authority). |
| `docs/meta/grammar-docs.md` | Style for learner-facing grammar prose (page skeleton, teach order, no process/corrective leftover text — pedagogy, not design authority). |
| `docs/phonology.md` | Phonology and phonotactics (linked from the language reference). |
| `docs/numbers.md` | Numeral grammar, including [digitless / zero-group forms](docs/numbers.md#zero-digit-groups) (`g+` = plural count `>1`; `/v/` `/h/` unspecified amount — **`v+`** *increase*, **`h+`** *multiple times*; …), [digitless exponents](docs/numbers.md#digitless-exponents) (`g+e` = +∞, `g+1e` = *one gazillion* hyperbole, `g#1e` = *the gazillionth* / *umpteenth*, `g#1e-` = *the gazillionth-first*, `g#-2` = *2nd from the end*, `g#-1` = *1st from the end* / *Starting with the last one*, `g#3e2` = *3rd of gen +2*, `g#e` = last place, `g#e-` = start/beginning place, **`x#e-`** = discourse *Starting with*, **`x#e`** = *Finally*; [zero × exponent](docs/numbers.md#zero-exponent) — **`g+0e`** absolute zero / **`g±0e-1`** engineering null·residue at OoM −1 / **`g#0e`** rank annihilated; [bare OoM bands](docs/numbers.md#bare-oom-bands) **`g+e0`** ones (1–9) / **`g+e3`** thousands (1000–9999); **`Ne0`** asserts OoM 0 ([ambient](docs/numbers.md#ambient-scale)); **`g#e0`** current generation; **`v+0el`** *annihilate* / **`j+0e`** *Annihilate!*; `/v/` `/h/` overlays — without-bound / ordinal landmarks / nudge-up / notional; **`j+e`** *To infinity!*, **`j#e`** *Finally!*, **`j#e-`** *(finally, we're) starting!*; **`g-e-`** = [notional / imaginary](docs/numbers.md#imaginary) (`v-e-` *notionalize*, `h-e-` *as if*, `j-e-` *As if!*, `x-e-` *imagine that:*); `x+e` / `x-e` undefined), [preferred writing](docs/numbers.md#writing-preferred-shorthand) / [style](docs/numbers.md#writing-style-numeric-vs-spelled) (second-slot **`~`** / **`@`** / **`=`** for **-m** / **-n** / **-r**; exact omits the ending letter), [percent denominators](docs/numbers.md#percent-denominators) (whole NP + `/ɡ/` `…jo`), [ranges](docs/numbers.md#ranges) (SHARED continuum `/ɡ/` + two number endpoints; bare `z+3 z+5 zel` = preference) and [measure phrases](docs/numbers.md#measure-phrases) (linked from the language reference). |
| `docs/pronouns.md` | Pronoun grammar (anaphoric **-r**; special **`umogo`** / **`ehado`** / **`ana`** / **`enu`** / **`odo`** — inclusive *we* = **`ana`**; [clusivity](docs/pronouns.md#clusivity) — linked from the language reference). |
| `docs/plurality.md` | Plural grammar (**-sh** = [associative](docs/plurality.md#associative) *anchor and associates* on nouns / event-sets / vocatives; **listener…sh** = [address set](docs/plurality.md#person-role-sh); **collective** on `/ɡ/`; unused on `/w/` `/h/` `/x/` and number words; generics / *every K* via joins, not **-sh** — [universals](docs/coordination.md#universals-domains-generics); habitual **`hual`** — [restrictors](docs/restrictors.md); linked from the language reference). |
| `docs/reference-suffix.md` | Reference suffix grammar (**-l** / **-m** / **-n** / **-r**; linked from the language reference). |
| `docs/meta/glosses.md` | Gloss writing guidelines (morph gloss = PoS + **English sense only**; literal / metaphorical / proper as separate roots; no Clarity letters; no redundant **-l** / **-m** / **-n**; free English defaults to **loose** idiomatic paraphrase — [strict vs loose](docs/meta/glosses.md#strict-vs-loose-free-english); linked from the language reference). |
| `docs/questions.md` | Interrogative grammar (**jol** / soft **jom** = yes/no **and** fill-ask; multi-gap **fill-all**; focus/bare under question; **polar stance** particles — **`jael`** / **`juel`** / **`jaol`** / **`juol`** / **`jual`** … — [yes/no polarity](docs/questions.md#yes-no-polarity); linked from the language reference). |
| `docs/spans.md` | Span fences (writing PoS + optional **`@`** / **`~`** after PoS + `[…]` / `{…}` / `(…)` / `<…>` (`d@[…]` proper, `d~[…]` paraphrase; hedged `@~` spelled `d@[…]`; anaphor **`d[=]`**); open `{PoS}{TYPE}x{EDGE}{ENDING}` — e.g. `daxal` / `daxol` / `daxan` / `daxar`; TYPE **a** cite / **e** aside / **o** mention / **u** opaque; EDGE **a** multi / **e** clause-scoped / **o** atomic / **u** empty; **-l** exact / **-m** paraphrase / **-n** proper / **-r** span anaphor; close **`xuxul`** / **`xuxur`** (`-]` truncated) / **`xuxun`** (`#]` sic) / **`xuxum`** (`\|` close-all; `-\|` / `#\|` = special + close-all); **adjunct-scope islands** **`^ … ^`** (prosody-only edges; binder inside: `/h/` and/or join; no nesting); linked from the language reference). |
| `docs/comparatives.md` | Scalar comparison via **rank joins**: **`e` / `oe` / `ue`** SHARED `/ɡ/` = [comparatives / superlatives](docs/comparatives.md) (`z<Sam>n z<Lea>n zel g<big>l` / focus *the …-est* / bare **`oe`** *no biggest*); **`ae`** SHARED scale = [equative](docs/comparatives.md#equatives); set **`a`** SHARED `/ɡ/` distributes (*both ADJ*); measured differentials use [measure phrases](docs/numbers.md#measure-phrases); [judgment benchmarks](docs/comparatives.md#judgment-benchmarks) (*worse than…*) = **`ue`** + **`zonugon`** / **`zaheman`** / **`zumogoxrawon`** / **`zulohun`** / **`zaloban`** / **`zuan`** (performance Mine stays bare **`zumogon`**); linked from the language reference. |
| `docs/predication.md` | **Classification** (kind / role as zero-copula predicative `/ɡ/`: `jal z<Sam>n gedagel`) and **identity** (closed complex **`SAME`** = **`oguno`** 🪙 + `/b/`: `jal z<Clark>n gogunol b<Superman>n`); no general *to-be* `/v/`; not scalar equative `ae`; linked from the language reference. |
| `docs/causation.md` | **Causation / condition** — necessary vs sufficient via existing joins (default open **`…aom`** / **`haon`**; necessity **`huan`/`guan`** / **`hal`**); clause poles = `/h/`+**`odo`** (**`orodo`** *if* / **`eluve`** *iff* / **`urugu`** *because*; not `xaom`-as-cause); optional **`CAUSE`** mood **`erage`**; preference-as-law stacks [values](docs/values.md); evidentiality / universality on causal claims; no cause-arrow word; linked from the language reference. |
| `docs/coordination.md` | **Joins** — phrase-, VP-, clause-level **right-close fence** only (left fences illegal); frame-first style = [frame echo](docs/coordination.md#frame-echo) (bare announce · echoed right-close); **set** vs **rank**; arity **bare** / **focus** / **list**; scope islands **`^ … ^`** (join inside); negation **distributes** (not De Morgan); **-r** member; clause soft **-n** (`xan` = *and then*); clause `/x/` inherits opener force; VP `-n` = [join-act verbs](docs/special-vocabulary.md#join-act-verbs); `/ɡ/` `/h/` `-n` = [join-relations](docs/special-vocabulary.md#join-relations); causal readings of inclusive / exclusion joins → [causation](docs/causation.md); linked from the language reference. |
| `docs/revisers.md` | Prefix-less **revisers** (**al** / **am** / **an** / **el** / … — in-clause *including* / *rather* / *instead* / *except*; discourse before force / omitted-default body / `/x/` continue / linker; *Starting with* = **`x#e-`**, *Finally* = **`x#e`**); linked from the language reference. |
| `docs/restrictors.md` | **`/h/`** / **`/w/`** **restrictors** (**hal**, **hual**, **har**, … — applicability, not sibling *and*); linked from the language reference. |
| `docs/values.md` | **Values** on ordinary **`/h/`** / **`/w/`** (default reading): need inventory; stances **topic** (bare) / **met `xa`** / **motive `xe`** / **prescription `xo`** / **unmet `xu`**; endings = **contact channel**, **preference standing**, **prescription force** (invite / endorse / commit / bound), or **changeability**; linked from the language reference. |
| `docs/special-vocabulary.md` | Special morphology / closed roots (not needs, not restrictors): **join-act verbs** (`van` / `von` / …); **join-relations** (`gan` / `han` / … — unary `/b/`); **role compounds** (**`a`/`u`/`o` x ROOT** = agent / patient / reltum — no *attacker* lexicon); **numeric derivation** (**`ROOT x NUM`** = essence / grain / void / [quasi](docs/special-vocabulary.md#quasi) (`ROOTx-e-`; [quasi-N](docs/special-vocabulary.md#quasi-n) `ROOTx-e-1` / `ROOTx-e-2`) / origin-telos / poly-de / **`+N`** strong digit morphs / **`-N`** privatives / ordinal **`#N`** / [zero×exp](docs/special-vocabulary.md#zero-exponent-derivation) **`+0e`/`±0e-1`/`#0e`** (free **`#e0`/`Ne0`** in numbers; no **`ROOTx+e0`/`+0e0`/`+Ne0`** morphs) / label-code / [infinite labels](docs/special-vocabulary.md#infinite-labels) **`_e`/`_e-`/`_1e`** / hyperbole — host ending only; bare `ROOTxN` illegal); **ability** / **incapability** — primary **host + `xu`/`xa`** + changeability endings (`v<sing>xun`); fallback closed **`egera`** (**`ABIL`**) when hostless; **plan / predict** (`owaro` / `edelo`), **evidentiality** (`uhunu` / `eroge` / `awa` / `eve` / `erare` / `ahe` / `orolo`), **universality** warrant (`ewono` / `ululu` / `idido` / `afafa` / `ebebe`), **emotion compose** (ACT + LOCUS + reused values), and **COMMENT** (mindfulness noting — **`uho`** *commentary*) = closed `/h/` mood roots (**not** `x`-compounds); **numbered alternatives** (ideation — **`ubezu` / `egoge` / `ogalo` + free `g#N`**; style, not new morph) — [numbered alternatives](docs/special-vocabulary.md#numbered-alternatives); closed **sense-form** overlays keyed by **`(sense_form, pos)`** — [sense-form](docs/special-vocabulary.md#sense-form) / [lexicon-overlays.csv](data/lexicon-overlays.csv); linked from the language reference. |
| `data/lexicon-published.csv` | Published emoji-seeded **roots** (literal / metaphorical / mnemonic). PoS and reference suffix applied at use time. |
| `data/lexicon-overlays.csv` | Closed overlay inventory: **`(sense_form, pos)`** rows with **`definition`** and **`mnemonic`** for special vocabulary only — [sense-form](docs/special-vocabulary.md#sense-form). |
| `docs/x-compounds.md` | Mid-word **`x`** **parser families** (span vs role vs values/ability vs numeric derivation vs ordinary compound) — linked from the language reference / phonology. |
| `TODO.md` | Planned major revision notes (PoS prefixes, word-final consonants, pronoun/number rules, phonology). Fold into the reference, `phonology.md`, and `pronouns.md` as work proceeds. |

## Psychological morphology

Clarity encodes psychologically useful distinctions natively (no English tag overlay). Values: **[docs/values.md](docs/values.md)**. Incapability: **[docs/special-vocabulary.md](docs/special-vocabulary.md#incapability)**. Role compounds (agent / patient / reltum): **[docs/special-vocabulary.md](docs/special-vocabulary.md#role-compounds)**. Numeric derivation (`ROOT x NUM`): **[docs/special-vocabulary.md](docs/special-vocabulary.md#numeric-derivation)**. Mid-word **`x`** families: **[docs/x-compounds.md](docs/x-compounds.md)**. Join-act verbs: **[docs/special-vocabulary.md](docs/special-vocabulary.md#join-act-verbs)**. Join-relations: **[docs/special-vocabulary.md](docs/special-vocabulary.md#join-relations)**. Plan / predict: **[docs/special-vocabulary.md](docs/special-vocabulary.md#plan-predict)**. Evidentiality: **[docs/special-vocabulary.md](docs/special-vocabulary.md#evidentiality)**. Universality: **[docs/special-vocabulary.md](docs/special-vocabulary.md#universality)**. Emotion compose: **[docs/special-vocabulary.md](docs/special-vocabulary.md#emotion-compose)**. Mindfulness noting / **COMMENT**: **[docs/special-vocabulary.md](docs/special-vocabulary.md#comment)**. Numbered alternatives (ideation): **[docs/special-vocabulary.md](docs/special-vocabulary.md#numbered-alternatives)**. Worse-than benchmarks: **[docs/comparatives.md](docs/comparatives.md#judgment-benchmarks)**. Causation: **[docs/causation.md](docs/causation.md)**.

## Tooling notes

- Node ≥ 20.
- Language design work goes through `docs/introduction.md` (purpose / feature criteria), `docs/language-reference.md` and the linked grammar docs (and `TODO.md` until absorbed). Do **not** treat deleted legacy ANTLR grammars or any future parsers as design authority until they match the docs.
- **Orthography:** actual Clarity has **no hyphen** after the PoS letter (`zumogon`, not `z-umogo`); foreign roots use `PoS<…>ENDING` (e.g. `d<english>l`); opaque spans take no ending after `>` (`d<sushi>`). Prefer published lexicon roots when the gloss matches; use **-m** for metaphorical gloss matches — see [language-reference.md § Orthography](docs/language-reference.md#orthography).
- **Utterance framing:** **`/j/`** = turn (vocatives, [polar stance](docs/questions.md#yes-no-polarity), marked force); **`/x/`** = same-force forward motion (clause joins, linkers — inherit force). Default assertoric **`jal`** is **omissible** when recoverable (period, polar **-l** / **-m**, `/x/` continue, same-force **`odo`** dependent). Do not require leading **`jal`** on every example sentence. Polar confirm tags are a **second bare turn** after `.` (`…. jael.`), not same-sentence final `/j/`.
- After editing Markdown under `docs/` (or `AGENTS.md` / `TODO.md`), run **`npm run lint:md`** — it fails on unclosed `*` / `**` and on slash-joined emphasis (`*a*/*b*`, which breaks Cursor’s rich preview with `italic,italic`). Prefer spaces (`*a* / *b*`) or one span (`*a/b*`). Avoid nesting `**…**` inside already-bold text; in bold headings/bullets put forms in backticks only (`**Ranked (`e` / `ae`)**`, not `**ae**` inside).
