# Clarity Language

Constructed language and tooling that encode psychologically useful distinctions into vocabulary and grammar (love / truth / freedom themes: gratitude, bias awareness, authentic choice). Unambiguous syntax supports computational tools; phonology aims to be easy to sing.

Always call the language **Clarity** (never Geran or other legacy names).

This repo is TypeScript + ANTLR tooling (`grammar/*.g4` → `src/generated/`). A web UI may come later.

## Sources of truth

| Source | Role |
|--------|------|
| `docs/language-reference.md` | **Source of truth** for the language design. Revise it as design decisions land. |
| `docs/phonology.md` | Phonology and phonotactics (linked from the language reference). |
| `docs/numbers.md` | Numeral grammar, including [digitless / zero-group forms](docs/numbers.md#zero-digit-groups) (`g+` = plural count `>1`; `/v/` `/h/` unspecified amount — **`v+`** *increase*, **`h+`** *multiple times*; …), [digitless exponents](docs/numbers.md#digitless-exponents) (`g+e` = +∞, `g+1e` = *one gazillion* hyperbole, `g#1e` = *the gazillionth* / *umpteenth*, `g#1e-` = *the gazillionth-first*, `g#-2` = *2nd from the end*, `g#-1` = *1st from the end* / *Starting with the last one*, `g#3e2` = *3rd of gen +2*, `g#e` = last place, `g#e-` = start/beginning place, **`x#e-`** = discourse *Starting with*, **`x#e`** = *Finally*; [zero × exponent](docs/numbers.md#zero-exponent) — **`g+0e`** absolute zero / **`g+e0`** unit amount / **`g+0e0`** `0⁰`; **`v+0el`** *annihilate* / **`v+e0`** *unitize* / **`j+0e`** *Annihilate!*; `/v/` `/h/` overlays — without-bound / ordinal landmarks / nudge-up / notional; **`j+e`** *To infinity!*, **`j#e`** *Finally!*, **`j#e-`** *(finally, we're) starting!*; **`g-e-`** = [notional / imaginary](docs/numbers.md#imaginary) (`v-e-` *notionalize*, `h-e-` *as if*, `j-e-` *As if!*, `x-e-` *imagine that:*); `x+e` / `x-e` undefined), [preferred writing](docs/numbers.md#writing-preferred-shorthand) (second-slot **`~`** / **`@`** / **`=`** for **-m** / **-n** / **-r**; exact omits the ending letter), [percent denominators](docs/numbers.md#percent-denominators) (whole NP + `/ɡ/` `…jo`), [ranges](docs/numbers.md#ranges) (SHARED continuum `/ɡ/` + two number endpoints; bare `z+3 z+5 zel` = preference) and [measure phrases](docs/numbers.md#measure-phrases) (linked from the language reference). |
| `docs/pronouns.md` | Pronoun grammar (linked from the language reference). |
| `docs/plurality.md` | Plural grammar (**-sh** = group referent on nouns / event-sets / vocatives; **collective** on `/ɡ/`; unused on `/w/` `/h/` `/x/` and number words; generics / *every K* via joins, not **-sh** — [universals](docs/coordination.md#universals-domains-generics); habitual **`hual`** — [restrictors](docs/restrictors.md); linked from the language reference). |
| `docs/reference-suffix.md` | Reference suffix grammar (**-l** / **-m** / **-n** / **-r**; linked from the language reference). |
| `docs/questions.md` | Interrogative grammar (**jol** / soft **jom** = yes/no **and** fill-ask; formerly *polar question*; multi-gap **fill-all**; focus/bare under question; **yes / no polarity** answer particles — **`jael`** (*yes* / *true*), **`juel`** (*no* / *false*), **`jaol`** / **`juol`** / **`jual`** … — [yes/no polarity](docs/questions.md#yes-no-polarity); linked from the language reference). |
| `docs/spans.md` | Span fences (writing PoS + optional **`@`** / **`~`** after PoS + `[…]` / `{…}` / `(…)` / `<…>` (`d@[…]` proper, `d~[…]` paraphrase; hedged `@~` spelled `d@[…]`; anaphor **`d[=]`**); open `{PoS}{TYPE}x{EDGE}{ENDING}` — e.g. `daxal` / `daxol` / `daxan` / `daxar`; TYPE **a** cite / **e** aside / **o** mention / **u** opaque; EDGE **a** multi / **e** clause-scoped / **o** atomic / **u** empty; **-l** exact / **-m** paraphrase / **-n** proper / **-r** span anaphor; close **`xuxul`** / **`xuxur`** (`-]` truncated) / **`xuxun`** (`#]` sic) / **`xuxum`** (`\|` close-all; `-\|` / `#\|` = special + close-all); **adjunct-scope islands** **`^ … ^`** (prosody-only edges; binder inside: `/h/` and/or join; no nesting); linked from the language reference). |
| `docs/comparatives.md` | Scalar comparison via **rank joins**: **`e` / `oe` / `ue`** SHARED `/ɡ/` = [comparatives / superlatives](docs/comparatives.md) (`z-Samn z-Lean zel g-bigl` / focus *the …-est* / bare **`oe`** *no biggest*); **`ae`** SHARED scale = [equative](docs/comparatives.md#equatives); set **`a`** SHARED `/ɡ/` distributes (*both ADJ*); measured differentials use [measure phrases](docs/numbers.md#measure-phrases); [judgment benchmarks](docs/comparatives.md#judgment-benchmarks) (*worse than Average|Typical|Mine|Social|Professional|Everyone*) = **`ue`** + closed **-n** comparee roots; linked from the language reference. |
| `docs/predication.md` | **Classification** (kind / role as zero-copula predicative `/ɡ/`: `jal z-Samn g-teacherl`) and **identity** (closed complex **`g-SAME`** + `/b/`: `jal z-Clarkn g-SAMEl b-Superman`); no general *to-be* `/v/`; not scalar equative `ae`; linked from the language reference. |
| `docs/causation.md` | **Causation / condition** — necessary vs sufficient via existing joins (default open **`…aom`** / **`haon`**; necessity **`huan`/`guan`** / **`hal`**); clause poles = `/h/`+NEXT (not `xaom`-as-cause); preference-as-law stacks [values](docs/values.md); Claritish `caused_` + evidentiality; no cause-arrow word; linked from the language reference. |
| `docs/coordination.md` | **Joins** — phrase-, VP-, clause-level **right-close fence** only (left fences illegal); **set** vs **rank**; arity **bare** / **focus** / **list**; scope islands **`^ … ^`** (join inside); negation **distributes** (not De Morgan); **-r** member; clause soft **-n** (`xan` = *and then*); VP `-n` = [join-act verbs](docs/special-vocabulary.md#join-act-verbs); `/ɡ/` `/h/` `-n` = [join-relations](docs/special-vocabulary.md#join-relations); causal readings of inclusive / exclusion joins → [causation](docs/causation.md); linked from the language reference. |
| `docs/revisers.md` | Prefix-less **revisers** (**al** / **am** / **an** / **el** / … — in-clause *including* / *rather* / *instead* / *except*; before `/j/` = discourse *additionally* / *in other words* / *instead* / *except*; *Starting with* = **`x#e-`**, *Finally* = **`x#e`**); linked from the language reference. |
| `docs/restrictors.md` | **`/h/`** / **`/w/`** **restrictors** (**hal**, **hual**, **har**, … — applicability, not sibling *and*); linked from the language reference. |
| `docs/values.md` | **Values** on ordinary **`/h/`** / **`/w/`** (default reading): need inventory; stances **topic** (bare) / **met `xa`** / **motive `xe`** / **prescription `xo`** / **unmet `xu`**; endings = **contact channel**, **preference standing**, **prescription force** (invite / endorse / commit / bound), or **changeability**; linked from the language reference. |
| `docs/special-vocabulary.md` | Special morphology / closed roots (not needs, not restrictors): **join-act verbs** (`van` / `von` / …); **join-relations** (`gan` / `han` / … — unary `/b/`); **role compounds** (**`a`/`u`/`o` x ROOT** = agent / patient / reltum — no *attacker* lexicon); **numeric derivation** (**`ROOT x NUM`** = essence / grain / void / [quasi](docs/special-vocabulary.md#quasi) (`ROOTx-e-`; [quasi-N](docs/special-vocabulary.md#quasi-n) `ROOTx-e-1` / `ROOTx-e-2`) / origin-telos / poly-de / **`+N`** strong digit morphs / **`-N`** privatives / ordinal **`#N`** / [zero×exp / identity](docs/special-vocabulary.md#zero-exponent-derivation) **`+0e`/`+e0`/`+0e0`** / label-code / [infinite labels](docs/special-vocabulary.md#infinite-labels) **`_e`/`_e-`/`_1e`** / hyperbole — host ending only; bare `ROOTxN` illegal); **ability** / **incapability** — primary **host + `xu`/`xa`** + changeability endings (`v-singxun`); fallback closed **`ABIL`** when hostless; **plan / predict** (`owaro` / `edelo`), **memory / record** past framing (`uhunu` / `eroge`), **emotion compose** (ACT + LOCUS + reused values), and **COMMENT** (mindfulness noting — **`uho`** *commentary*) = closed `/h/` mood roots (**not** `x`-compounds); linked from the language reference. |
| `docs/x-compounds.md` | Mid-word **`x`** **parser families** (span vs role vs values/ability vs numeric derivation vs ordinary compound) — linked from the language reference / phonology. |
| `TODO.md` | Planned major revision notes (PoS prefixes, word-final consonants, pronoun/number rules, phonology). Fold into the reference, `phonology.md`, and `pronouns.md` as work proceeds. |
| `docs/claritish-rules.js` | English “Claritish” regex enforcers (value tags, evidentiality, mindfulness noting, etc.). **To be ported** into Clarity (lexicon/grammar), not kept as an English overlay forever. |
| `grammar/*.g4` | Existing ANTLR parsers for tooling experiments. **Do not change for now.** They may lag the reference; do not treat them as design authority. |

## Claritish → Clarity

`claritish-rules.js` defines English-side constraints that force explicit tagging (e.g. `my+c`, `caused_i`, `always_c`, emotion decompose). The long-term goal is that Clarity’s own words and morphology carry those distinctions natively so speakers don’t need English suffixes. Values: **[docs/values.md](docs/values.md)**. Incapability: **[docs/special-vocabulary.md](docs/special-vocabulary.md#incapability)**. Role compounds (agent / patient / reltum): **[docs/special-vocabulary.md](docs/special-vocabulary.md#role-compounds)**. Numeric derivation (`ROOT x NUM`): **[docs/special-vocabulary.md](docs/special-vocabulary.md#numeric-derivation)**. Mid-word **`x`** families: **[docs/x-compounds.md](docs/x-compounds.md)**. Join-act verbs: **[docs/special-vocabulary.md](docs/special-vocabulary.md#join-act-verbs)**. Join-relations: **[docs/special-vocabulary.md](docs/special-vocabulary.md#join-relations)**. Plan / predict: **[docs/special-vocabulary.md](docs/special-vocabulary.md#plan-predict)**. Memory / record (past framing): **[docs/special-vocabulary.md](docs/special-vocabulary.md#memory-record)**. Emotion compose: **[docs/special-vocabulary.md](docs/special-vocabulary.md#emotion-compose)**. Mindfulness noting / **COMMENT**: **[docs/special-vocabulary.md](docs/special-vocabulary.md#comment)**. Worse-than benchmarks: **[docs/comparatives.md](docs/comparatives.md#judgment-benchmarks)**. Causation / `caused_`: **[docs/causation.md](docs/causation.md)**.

## Tooling notes

- Node ≥ 20; `npm run generate` / `npm run parse` exist for the current grammars — leave grammars alone unless explicitly asked.
- Generated parsers live in `src/generated/` (gitignored).
- Language design work goes through `docs/language-reference.md` (and `TODO.md` until absorbed).
- After editing Markdown under `docs/` (or `AGENTS.md` / `TODO.md`), run **`npm run lint:md`** — it fails on unclosed `*` / `**` and on slash-joined emphasis (`*a*/*b*`, which breaks Cursor’s rich preview with `italic,italic`). Prefer spaces (`*a* / *b*`) or one span (`*a/b*`). Avoid nesting `**…**` inside already-bold text; in bold headings/bullets put forms in backticks only (`**Ranked (`e` / `ae`)**`, not `**ae**` inside).
