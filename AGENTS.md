# Agelan

Constructed language and tooling that encode psychologically useful distinctions into vocabulary and grammar (compassion / rationality / empowerment themes: gratitude, bias awareness, authentic choice). Unambiguous syntax supports computational tools; phonology aims to be easy to sing.

Always call the language **Agelan** (never Clarity, Geran, or other legacy names). Do not restate the English gloss from [`docs/grammar/introduction.md`](docs/grammar/introduction.md).

This repo is TypeScript tooling around the language docs and lexicon. A web UI may come later.

## Sources of truth

| Source | Role |
|--------|------|
| `docs/grammar/introduction.md` | Purpose (compassion / rationality / empowerment), grammar design goals, learning path, feature criteria. |
| `docs/grammar/core.md` | **Source of truth** for core sentence grammar (PoS, orthography, utterance framing, `/x/` continue, **`odo`**). Other grammar docs own their subsystems. |
| `docs/meta/learning-levels.md` | Rubric for tagging grammar docs **beginner** / **intermediate** / **advanced** and the [cross-doc Beginner path](docs/meta/learning-levels.md#cross-doc-path) (pedagogy, not design authority). |
| `docs/meta/grammar-docs.md` | Style for learner-facing grammar prose under `docs/grammar/` only (page skeleton, teach order, no process leftover — pedagogy, not design authority). |
| `docs/grammar/phonology.md` | Phonology and phonotactics (linked from the core grammar). |
| `docs/grammar/numbers.md` | Numeral grammar, including [digitless / zero-group forms](docs/grammar/numbers.md#zero-digit-groups) (`g+` = plural count `>1`; `/v/` `/h/` unspecified amount — **`v+`** *increase*, **`h+`** *multiple times*; …), [digitless exponents](docs/grammar/numbers.md#digitless-exponents) (`g+e` = +∞, `g+1e` = *one gazillion* hyperbole, `g#1e` = *the gazillionth* / *umpteenth*, `g#1e-` = *the gazillionth-first*, `g#-2` = *2nd from the end*, `g#-1` = *1st from the end* / *Starting with the last one*, `g#3e2` = *3rd of gen +2*, `g#e` = last place, `g#e-` = start/beginning place, **`x#e-`** = discourse *Starting with*, **`x#e`** = *Finally*; [zero × exponent](docs/grammar/numbers.md#zero-exponent) — **`g+0e`** absolute zero / **`g±0e-1`** engineering null·residue at OoM −1 / **`g#0e`** rank annihilated; [bare OoM bands](docs/grammar/numbers.md#bare-oom-bands) **`g+e0`** ones (1–9) / **`g+e3`** thousands (1000–9999); **`Ne0`** asserts OoM 0 ([ambient](docs/grammar/numbers.md#ambient-scale)); **`g#e0`** current generation; **`v+0el`** *annihilate* / **`j+0e`** *Annihilate!*; `/v/` `/h/` overlays — without-bound / ordinal landmarks / nudge-up / notional; **`j+e`** *To infinity!*, **`j#e`** *Finally!*, **`j#e-`** *(finally, we're) starting!*; **`g-e-`** = [notional / imaginary](docs/grammar/numbers.md#imaginary) (`v-e-` *notionalize*, `h-e-` *as if*, `j-e-` *As if!*, `x-e-` *imagine that:*); `x+e` / `x-e` undefined), [preferred writing](docs/grammar/numbers.md#writing-preferred-shorthand) / [style](docs/grammar/numbers.md#writing-style-numeric-vs-spelled) (second-slot **`~`** / **`@`** / **`=`** for **-m** / **-n** / **-r**; exact omits the ending letter), [percent denominators](docs/grammar/numbers.md#percent-denominators) (whole NP + `/ɡ/` `…jo`), [ranges](docs/grammar/numbers.md#ranges) (SHARED continuum `/ɡ/` + two number endpoints; bare `z+3 z+5 zel` = preference) and [measure phrases](docs/grammar/numbers.md#measure-phrases) (bare metaphorical host = short unit; titled **`ROOTx+1`**; no metric prefixes / Latin abbreviations — linked from the core grammar). |
| `docs/grammar/pronouns.md` | Pronoun grammar (anaphoric **-r**; special **`umogo`** / **`ehado`** / **`ana`** / **`enu`** / **`odo`** — inclusive *we* = **`ana`**; [clusivity](docs/grammar/pronouns.md#clusivity) — linked from the core grammar). |
| `docs/grammar/plurality.md` | Plural grammar (**-sh** = [associative](docs/grammar/plurality.md#associative) *anchor and associates* on nouns / event-sets / vocatives; **listener…sh** = [address set](docs/grammar/plurality.md#person-role-sh); **collective** on `/ɡ/`; unused on `/w/` `/h/` `/x/` and number words; generics / *every K* via joins, not **-sh** — [universals](docs/grammar/coordination.md#universals-domains-generics); habitual **`hual`** — [restrictors](docs/grammar/restrictors.md); linked from the core grammar). |
| `docs/grammar/reference-suffix.md` | Reference suffix grammar (**-l** / **-m** / **-n** / **-r**; linked from the core grammar). |
| `docs/meta/glosses.md` | Gloss writing guidelines (morph gloss = PoS + **English sense only**; literal / metaphorical / proper as separate roots; no Agelan letters; no redundant **-l** / **-m** / **-n**; free English defaults to **loose** idiomatic paraphrase — [strict vs loose](docs/meta/glosses.md#strict-vs-loose-free-english); linked from the core grammar). |
| `docs/grammar/questions.md` | Interrogative grammar (**jol** / soft **jom** = yes/no **and** fill-ask; multi-gap **fill-all**; focus/bare under question; **polar stance** particles — **`jael`** / **`juel`** / **`jaol`** / **`juol`** / **`jual`** … — [yes/no polarity](docs/grammar/questions.md#yes-no-polarity); linked from the core grammar). |
| `docs/grammar/spans.md` | Span fences (writing PoS + optional **`@`** / **`~`** after PoS + `[…]` / `{…}` / `(…)` / `<…>` (`d@[…]` proper, `d~[…]` paraphrase; hedged `@~` spelled `d@[…]`; anaphor **`d[=]`**); open `{PoS}{TYPE}x{EDGE}{ENDING}` — e.g. `daxal` / `daxol` / `daxan` / `daxar`; TYPE **a** cite / **e** aside / **o** mention / **u** opaque; EDGE **a** multi / **e** clause-scoped / **o** atomic / **u** empty; **-l** exact / **-m** paraphrase / **-n** proper / **-r** span anaphor; close **`xuxul`** / **`xuxur`** (`-]` truncated) / **`xuxun`** (`#]` sic) / **`xuxum`** (`\|` close-all; `-\|` / `#\|` = special + close-all); **adjunct-scope islands** **`^ … ^`** (prosody-only edges; binder inside: `/h/` and/or join; no nesting); linked from the core grammar). |
| `docs/grammar/comparatives.md` | Scalar comparison via **rank joins**: **`e` / `oe` / `ue`** SHARED `/ɡ/` = [comparatives / superlatives](docs/grammar/comparatives.md) (`z<Sam>n z<Lea>n zel g<big>l` / focus *the …-est* / bare **`oe`** *no biggest*); **`ae`** SHARED scale = [equative](docs/grammar/comparatives.md#equatives); set **`a`** SHARED `/ɡ/` distributes (*both ADJ*); measured differentials use [measure phrases](docs/grammar/numbers.md#measure-phrases); [judgment benchmarks](docs/grammar/comparatives.md#judgment-benchmarks) (*worse than…*) = **`ue`** + **`zonugon`** / **`zaheman`** / **`zumogoxrawon`** / **`zulohun`** / **`zaloban`** / **`zuan`** (performance Mine stays bare **`zumogon`**); linked from the core grammar. |
| `docs/grammar/predication.md` | **Classification** (kind / role as zero-copula predicative `/ɡ/`: `jal z<Sam>n gedagel`) and **identity** (closed complex **`SAME`** = **`oguno`** 🪙 + `/b/`: `jal z<Clark>n gogunol b<Superman>n`); no general *to-be* `/v/`; not scalar equative `ae`; linked from the core grammar. |
| `docs/grammar/causation.md` | **Causation / condition** — necessary vs sufficient via existing joins (default open **`…aom`** / **`haon`**; necessity **`huan`/`guan`** / **`hal`**); clause poles = `/h/`+**`odo`** (**`orodo`** *if* / **`eluve`** *iff* / **`urugu`** *because*; not `xaom`-as-cause); optional **`CAUSE`** mood **`erage`**; preference-as-law stacks [values](docs/grammar/values.md); evidentiality / universality on causal claims; no cause-arrow word; linked from the core grammar. |
| `docs/grammar/coordination.md` | **Joins** — phrase-, VP-, clause-level **right-close fence** only (left fences illegal); frame-first style = [frame echo](docs/grammar/coordination.md#frame-echo) (bare announce · echoed right-close); **set** vs **rank**; arity **bare** / **focus** / **list**; scope islands **`^ … ^`** (join inside); negation **distributes** (not De Morgan); **-r** member; clause soft **-n** (`xan` = *and then*); clause `/x/` inherits opener force; VP `-n` = [join-act verbs](docs/grammar/special-vocabulary.md#join-act-verbs); `/ɡ/` `/h/` `-n` = [join-relations](docs/grammar/special-vocabulary.md#join-relations); causal readings of inclusive / exclusion joins → [causation](docs/grammar/causation.md); linked from the core grammar. |
| `docs/grammar/revisers.md` | Prefix-less **revisers** (**al** / **am** / **an** / **el** / … — in-clause *including* / *rather* / *instead* / *except*; discourse before force / omitted-default body / `/x/` continue / linker; *Starting with* = **`x#e-`**, *Finally* = **`x#e`**); linked from the core grammar. |
| `docs/grammar/restrictors.md` | **`/h/`** / **`/w/`** **restrictors** (**hal**, **hual**, **har**, … — applicability, not sibling *and*); linked from the core grammar. |
| `docs/grammar/values.md` | **Values** on ordinary **`/h/`** / **`/w/`** (default reading): need inventory; stances **topic** (bare) / **met `xa`** / **motive `xe`** / **prescription `xo`** / **unmet `xu`**; endings = **contact channel**, **preference standing**, **prescription force** (invite / endorse / commit / bound), or **changeability**; linked from the core grammar. |
| `docs/grammar/special-vocabulary.md` | Special morphology / closed roots (not needs, not restrictors): **join-act verbs** (`van` / `von` / …); **join-relations** (`gan` / `han` / … — unary `/b/`); **role compounds** (**`a`/`u`/`o` x ROOT** = agent / patient / reltum — no *attacker* lexicon); **numeric derivation** (**`ROOT x NUM`** = essence / grain / void / [quasi](docs/grammar/special-vocabulary.md#quasi) (`ROOTx-e-`; [quasi-N](docs/grammar/special-vocabulary.md#quasi-n) `ROOTx-e-1` / `ROOTx-e-2`) / origin-telos / poly-de / **`+N`** strong digit morphs / **`-N`** privatives / ordinal **`#N`** / [zero×exp](docs/grammar/special-vocabulary.md#zero-exponent-derivation) **`+0e`/`±0e-1`/`#0e`** (free **`#e0`/`Ne0`** in numbers; no **`ROOTx+e0`/`+0e0`/`+Ne0`** morphs) / label-code / [infinite labels](docs/grammar/special-vocabulary.md#infinite-labels) **`_e`/`_e-`/`_1e`** / hyperbole — host ending only; bare `ROOTxN` illegal); **ability** / **incapability** — primary **host + `xu`/`xa`** + changeability endings (`v<sing>xun`); fallback closed **`egera`** (**`ABIL`**) when hostless; **plan / predict** (`owaro` / `edelo`), **DECISION** (**`ehege`** *decision* + changeability endings), **evidentiality** (`uhunu` / `eroge` / `awa` / `eve` / `erare` / `ahe` / `orolo`), **universality** warrant (`ewono` / `ululu` / `idido` / `afafa` / `ebebe`), **emotion compose** (ACT + LOCUS + reused values), and **COMMENT** (mindfulness noting — **`uho`** *commentary* + hold endings) / **NOTIONAL** (**`edahe`** *pretense* / as-if + same hold map) = closed `/h/` mood roots (**not** `x`-compounds; not free number notional forms); **numbered alternatives** (ideation — **`ubezu` / `egoge` / `ogalo` + free `g#N`**; style, not new morph) — [numbered alternatives](docs/grammar/special-vocabulary.md#numbered-alternatives); closed **sense-form** overlays keyed by **`(sense_form, pos)`** — [sense-form](docs/grammar/special-vocabulary.md#sense-form) / [lexicon-overlays.csv](data/lexicon-overlays.csv); linked from the core grammar. |
| `data/lexicon-published.csv` | Published emoji-seeded **roots** (literal / metaphorical / mnemonic). PoS and reference suffix applied at use time. |
| `data/lexicon-overlays.csv` | Closed overlay inventory: **`(sense_form, pos)`** rows with **`definition`** and **`mnemonic`** for special vocabulary only — [sense-form](docs/grammar/special-vocabulary.md#sense-form). |
| `docs/grammar/x-compounds.md` | Mid-word **`x`** **parser families** (span vs role vs values/ability vs numeric derivation vs ordinary compound) — linked from the core grammar / phonology. |
| `TODO.md` | Planned major revision notes (PoS prefixes, word-final consonants, pronoun/number rules, phonology). Fold into `docs/grammar/core.md`, `docs/grammar/phonology.md`, and `docs/grammar/pronouns.md` as work proceeds. |

## Psychological morphology

Agelan encodes psychologically useful distinctions natively (no English tag overlay). Values: **[docs/grammar/values.md](docs/grammar/values.md)**. Incapability: **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#incapability)**. Role compounds (agent / patient / reltum): **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#role-compounds)**. Numeric derivation (`ROOT x NUM`): **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#numeric-derivation)**. Mid-word **`x`** families: **[docs/grammar/x-compounds.md](docs/grammar/x-compounds.md)**. Join-act verbs: **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#join-act-verbs)**. Join-relations: **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#join-relations)**. Plan / predict: **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#plan-predict)**. Decision / revisability (**DECISION**): **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#decision)**. Evidentiality: **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#evidentiality)**. Universality: **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#universality)**. Emotion compose: **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#emotion-compose)**. Mindfulness noting / **COMMENT**: **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#comment)**. Notional / as-if (**NOTIONAL**): **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#notional)**. Numbered alternatives (ideation): **[docs/grammar/special-vocabulary.md](docs/grammar/special-vocabulary.md#numbered-alternatives)**. Worse-than benchmarks: **[docs/grammar/comparatives.md](docs/grammar/comparatives.md#judgment-benchmarks)**. Causation: **[docs/grammar/causation.md](docs/grammar/causation.md)**.

## Tooling notes

- Node ≥ 20.
- Language design work goes through `docs/grammar/introduction.md` (purpose / feature criteria), `docs/grammar/core.md` and the linked grammar docs (and `TODO.md` until absorbed). Do **not** treat deleted legacy ANTLR grammars or any future parsers as design authority until they match the docs.
- **Orthography:** actual Agelan has **no hyphen** after the PoS letter (`zumogon`, not `z-umogo`); foreign roots use `PoS<…>ENDING` (e.g. `d<english>l`); opaque spans take no ending after `>` (`d<sushi>`). Prefer published lexicon roots when the gloss matches; use **-m** for metaphorical gloss matches — see [core.md § Orthography](docs/grammar/core.md#orthography). Outside a sentence, prefix-less **root + ending** is [citation](docs/grammar/core.md#citation-forms) (not a default `/z/`); closed **`al` / `am` / …** stay [revisers](docs/grammar/revisers.md).
- **Utterance framing:** **`/j/`** = turn (vocatives, [polar stance](docs/grammar/questions.md#yes-no-polarity), marked force); **`/x/`** = same-force forward motion (clause joins, linkers — inherit force). Default assertoric **`jal`** is **omissible** when recoverable (period, polar **-l** / **-m**, `/x/` continue, same-force **`odo`** dependent). Do not require leading **`jal`** on every example sentence. Polar confirm tags are a **second bare turn** after `.` (`…. jael.`), not same-sentence final `/j/`.
- After editing Markdown under `docs/` (or `AGENTS.md` / `TODO.md`), run **`npm run lint:md`** — it fails on unclosed `*` / `**`, slash-joined emphasis (`*a*/*b*`, which breaks Cursor’s rich preview with `italic,italic`), and broken internal links / anchors. Prefer spaces (`*a* / *b*`) or one span (`*a/b*`). Avoid nesting `**…**` inside already-bold text; in bold headings/bullets put forms in backticks only (`**Ranked (`e` / `ae`)**`, not `**ae**` inside).
